{
  "name": "brain-plugin",
  "version": "0.4.0",
  "private": true,
  "license": "MIT",
  "description": "Markdown-first AI-assisted second brain for Obsidian.",
  "keywords": [
    "obsidian",
    "plugin",
    "markdown",
    "ai"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "lint": "tsc --noEmit",
    "test": "node scripts/run-smoke-tests.mjs",
    "smoke:installed": "node scripts/installed-smoke-tests.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.7.7",
    "typescript": "^5.8.3"
  },
  "peerDependencies": {
    "obsidian": ">=1.7.0"
  }
}
